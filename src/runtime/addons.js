// FacturaScripts plugins stub.
// For the initial playground, addon materialization is not needed.
// Plugins are bundled in the VFS or placed in the mutable Plugins directory.

export async function materializeBlueprintAddons({ php, blueprint, fsRoot, publish, config }) {
  return {
    plugins: [],
  };
}
